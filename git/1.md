#常用命令

git pull 
git pull --rebase 
git pull 
git checkout
git branch 
git switch


```bash

# 退回到上一个版本
git reset --hard HEAD^  
# 退三个版本
git reset --hard HEAD~3
# 退到指定commit
git reset --hard commit_id

```

